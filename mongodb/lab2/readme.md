# actividad

## bajo la base de datos 'si3009db' y utilizando mongoCLI (mongoshell) y mongodb compass, realizar la carga de datos directo a la base de datos desde el archivo facturas.json y desde el CLI con facturas-comandos-mongodb.js. 